# To-Do-List-Javascript
Creating a HTML page. divs, class, id, list, input field and a button.
Styling it via CSS.  creating a stilized li element before hand and then adding it via javascript later
Creating variables to interact with our html page, the input-box and list-container.
Adding an Alert for no text in a input field, adding the elements as li if there is something entered, adding a stilized button like hover cross as a way to delete the rows in the list later, adding a way for the box to be empty after entering a list item.

Adding a fucntion to check where the user has clicked and to add the .name extension to a specific element in html, and having th css pick it up with a specific style when added, wether it is to remove something or to add to its style.

Adding a function to save our list items and our choices to our browsers then re-adding that specific code into the rest of the functions so it calls upon the save at the end of the code execution, and finally adding the function for the browser to recall all of this at the very end of the code.

